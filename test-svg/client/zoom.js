// //zoom Behaviour, selects, Zoombehavior(which is the svgFile), appens the zoom behavior on a scale from 1,6. And applies zoom functionality on zooming.
$(function () {
// var svg = $('#svg').children().first();
// var svgDep = new Deps.Dependency;
//
// var getSvg = function () {
// 	svgDep.depend();
// 	return svg;
// };
//
// var setSvg = function (){
// 	svgDep.changed();
// 	return
// }
//  d3.select('#zoomBehavior').call(d3.behavior.zoom().scaleExtent([1, 6]).on("zoom", zoom));
// // sets transform and translate attributes,  adding Zoomlevel to entire DOM, so can apply Zoom styling properties. D3.event.scale = gets the level of zoom. appending this to all of the svg contents.
// function zoom() {
// 	d3.select('#svg').attr("transform", "translate(" + d3.event.translate + ")scale(" + d3.event.scale + ")");
// 	var zoomlevel = d3.event.scale;
// 	d3.select('#svg').selectAll("*").attr('zoom', Math.round(zoomlevel));
// }

});
